# aws-cicd-pipeline
Built a fully automated CI/CD pipeline on AWS that takes code from GitHub, builds it into a Docker container, stores the image in ECR, and deploys it to a live EKS cluster — no manual steps involved. Includes a manual approval gate before deployment, CloudWatch monitoring, and SNS alerts on pipeline events.


#test 